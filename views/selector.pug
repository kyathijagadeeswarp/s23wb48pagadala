extends layout

block content
  h1= title
  - var image_names = ["boots.jpeg", "boots1.jpg", "ff1.jpg", "ff2.jpg", "sandals.jpg", ""]
  - for(let i = 0; i < 3; i++) {
    - let number = Math.floor(Math.random() * 5)
    img(src=`/images/${image_names[number]}`, alt="", height="300px", width="300px")
  - } 